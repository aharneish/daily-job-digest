# Core dependencies (already in your project)
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
python-dotenv>=1.0.0

# 🤖 NEW: LangChain and Groq AI dependencies
langchain>=0.1.0
langchain-groq>=0.1.0

# 📄 NEW: Resume file format support (optional)
python-docx>=1.1.0  # For .docx files
PyPDF2>=3.0.0       # For .pdf files

# 📊 Data processing (if you want to enhance CSV handling)
pandas>=2.0.0       # Optional: for advanced CSV processing

# 🔒 Security (recommended)
cryptography>=41.0.0  # For secure API key handling
